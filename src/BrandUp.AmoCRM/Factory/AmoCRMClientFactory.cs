namespace BrandUp.AmoCRM.Factory
{
    public class AmoCRMClientFactory : IAmoCRMClientFactory
    {
        //readonly IHttpClientFactory factory;
        //readonly ILogger<AmoCRMBaseClient> logger;

        //public AmoCRMClientFactory(IHttpClientFactory factory, ILogger<AmoCRMBaseClient> logger)
        //{
        //    this.factory = factory ?? throw new ArgumentNullException(nameof(factory));
        //    this.logger = logger ?? throw new ArgumentNullException(nameof(logger));
        //}

        //public IAmoCRMBaseClient CreateClient()
        //{
        //    return new AmoCRMBaseClient(factory.CreateClient(), logger);
        //}
    }
}
